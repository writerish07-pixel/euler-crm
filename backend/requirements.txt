fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
# pymongo 4.5 / motor 3.3 crash on Railway's default Python 3.13
# (C extensions bson/_cbson/pymongo._cmessage). 4.9+ and motor 3.6+ support 3.13;
# pin files MUST live in this backend/ folder too: Railway's service root is
# backend/, so a repo-root .python-version is invisible to Railpack and it
# keeps installing 3.13.15.
pymongo==4.13.2
motor==3.7.1
pydantic>=2.6.4
python-multipart>=0.0.9
# Lead bulk upload (template + .xlsx parsing) and GET /api/export.
openpyxl>=3.1.2
httpx>=0.27.0
# Phone notifications for the installed PWA (lead approval). Optional at runtime.
pywebpush>=2.0.3


bcrypt>=4.1.2
PyJWT>=2.8.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
