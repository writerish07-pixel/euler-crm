# Production API. CRA inlines REACT_APP_* at build time; without this the bundle
# would compile REACT_APP_BACKEND_URL as undefined and every request would go to
# "undefined/api". No secrets belong in this file — it ships to the browser.
REACT_APP_BACKEND_URL=https://euler-crm-api.onrender.com
