{
  "name": "euler-crm-repo",
  "private": true,
  "version": "1.0.0",
  "description": "Repo root. The deployable frontend lives in frontend/ (Create React App). These scripts delegate there so a build run from the repository root still works.",
  "scripts": {
    "build": "cd frontend && yarn install --frozen-lockfile && yarn build",
    "start": "cd frontend && yarn start"
  }
}
