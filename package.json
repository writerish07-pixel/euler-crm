{
  "name": "euler-crm-repo",
  "private": true,
  "version": "1.0.0",
  "description": "Repo root. Frontend is Create React App under frontend/. Root scripts exist so Cloudflare Workers Builds (npm run build → wrangler deploy) and other root-level CI can build without a custom rootDir.",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix frontend",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
